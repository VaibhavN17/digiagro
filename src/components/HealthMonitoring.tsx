"use client";

export default function HealthMonitoring({ farmerId }: { farmerId: string }) {
  return (
    <div>
      <h2>AI Health Monitoring</h2>
      <p>Smart camera alerts and early disease detection</p>
      {/* Add health alerts, camera feeds, etc. */}
    </div>
  );
}