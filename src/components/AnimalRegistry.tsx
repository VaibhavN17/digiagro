"use client";

export default function AnimalRegistry({ farmerId }: { farmerId: string }) {
  return (
    <div>
      <h2>Animal Registry</h2>
      <p>RFID/NFC based digital health records</p>
      {/* Add animal list, search, filter components */}
    </div>
  );
}